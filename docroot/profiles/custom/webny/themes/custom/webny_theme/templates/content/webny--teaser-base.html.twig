<article{{ attributes.addClass('webny-teaser') }}>
    <div class="webny-teaser-wrapper">
        <a class="webny-teaser-image" href="{{ url }}">
            {% block hero_image %}{% endblock hero_image %}
        </a>
        <div class="webny-teaser-content-wrapper">
            <div class="webny-teaser-filter-terms">
                {% for k,v in node.field_webny_filter_terms.value %}
                {% if content.field_webny_filter_terms[k] %}
                    
                          {% if loop.last %}
                            {% set separator = '' %}
                          {% else %}
                            {% set separator = ', ' %}
                          {% endif %}
                          {{ content.field_webny_filter_terms[k] }}{{ separator }}
                {% endif %}
            {% endfor %}
            </div>
            <span class="webny-teaser-filter-term-border"></span>
            <div class="webny-teaser-title">
                {% block title %}
                    <a href="{{ url }}">
                        {% if title|length <= 65 %}
                            {{ title }}
                        {% else %}
                            {{ title|slice(0, 65) ~ '...' }}
                        {% endif %}
                    </a>
                {% endblock title %}
            </div>
            {% block content %}{% endblock content %}
        </div>
        {{ content.links }}
    </div>
</article>
